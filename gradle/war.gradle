apply plugin: "war"

bootWar {
    mainClass = "com.funderse.api.FunderseApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
